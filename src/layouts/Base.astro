---
import "@fontsource/noto-sans-jp"
import "@fontsource/biz-udpmincho"
import "../styles/global.css"
import { site } from "../site"

interface Props {
  title?: string
  description?: string
  image?: string
}

const title = Astro.props.title
  ? `${Astro.props.title} | ${site.title}`
  : site.title
const description = Astro.props.description ?? site.description
const image = new URL(
  Astro.props.image ?? "/cat-solid.png",
  site.url,
).toString()
const isHome = Astro.url.pathname === "/"
const isTags = Astro.url.pathname.startsWith("/tags")
---

<!doctype html>
<html lang="ja" class="scroll-smooth">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:creator" content={`@${site.social.twitter}`} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />
    <link rel="icon" type="image/png" href="/cat-solid.png" />
    <link rel="shortcut icon" type="image/png" href="/cat-solid.png" />
    <link
      rel="alternate"
      type="application/rss+xml"
      title={site.title}
      href="/rss.xml"
    />
    <title>{title}</title>
  </head>
  <body class="min-h-screen overflow-x-hidden text-[var(--ink)] antialiased selection:bg-[var(--marker)] selection:text-[var(--ink)]">
    <a
      class="sr-only focus:not-sr-only focus:fixed focus:left-3 focus:top-3 focus:z-50 focus:rounded-full focus:bg-white focus:px-4 focus:py-2 focus:text-sm focus:font-black focus:text-slate-950 focus:no-underline focus:shadow-xl"
      href="#main-content">本文へ移動</a
    >
    <div class="site-sheet" aria-hidden="true"></div>
    <header class="sticky top-0 z-50 border-b border-[var(--rule)] bg-[var(--paper)]/90 backdrop-blur-xl">
      <div
        class="mx-auto flex min-h-16 max-w-6xl items-center justify-between gap-4 px-4"
      >
        <a
          class="inline-flex items-center gap-3 text-lg font-black tracking-normal text-[var(--ink)] no-underline sm:text-xl"
          href="/"
          aria-label={site.title}
        >
          <span
            class="grid size-10 place-items-center rounded-lg border border-[var(--ink)] bg-[var(--marker)] text-[var(--ink)] shadow-[4px_4px_0_var(--ink)]"
          >
            <img
              src="/cat-solid.png"
              alt=""
              width="24"
              height="24"
              class="size-6 object-contain"
            />
          </span>
          <span>{site.title}</span>
        </a>
        <nav
          class="flex items-center gap-1 max-[520px]:hidden"
          aria-label="Primary"
        >
          <a
            class:list={[
              "rounded-md px-3 py-2 text-sm font-bold no-underline transition hover:bg-[var(--ink)] hover:text-[var(--paper)]",
              isHome
                ? "bg-[var(--ink)] text-[var(--paper)]"
                : "text-[var(--muted)]",
            ]}
            href="/"
            aria-current={isHome ? "page" : undefined}>Articles</a
          >
          <a
            class:list={[
              "rounded-md px-3 py-2 text-sm font-bold no-underline transition hover:bg-[var(--ink)] hover:text-[var(--paper)]",
              isTags
                ? "bg-[var(--ink)] text-[var(--paper)]"
                : "text-[var(--muted)]",
            ]}
            href="/tags/"
            aria-current={isTags ? "page" : undefined}>Tags</a
          >
          <a
            class="rounded-md px-3 py-2 text-sm font-bold text-[var(--muted)] no-underline transition hover:bg-[var(--ink)] hover:text-[var(--paper)] max-[420px]:hidden"
            href="/rss.xml">RSS</a
          >
        </nav>
      </div>
    </header>
    <div
      class="mx-auto max-w-6xl px-4 py-10 sm:py-14"
      data-is-root-path={Astro.url.pathname === "/"}
    >
      <main id="main-content" tabindex="-1">
        <slot />
      </main>
      <footer
        class="mx-auto mt-16 flex max-w-3xl flex-wrap justify-between gap-3 border-t border-[var(--rule)] pt-6 text-sm text-[var(--muted)]"
      >
        <span>© {new Date().getFullYear()} ochisamu</span>
        <span>Built with <a href="https://astro.build">Astro</a></span>
      </footer>
    </div>
    <script>
      const enhanceCodeBlocks = () => {
        document.querySelectorAll("pre.astro-code").forEach(pre => {
          if (pre.parentElement?.classList.contains("code-block")) return

          const title = pre.previousElementSibling?.classList.contains(
            "code-title",
          )
            ? pre.previousElementSibling.textContent?.trim()
            : ""
          pre.previousElementSibling?.classList.contains("code-title") &&
            pre.previousElementSibling.remove()

          const wrapper = document.createElement("div")
          wrapper.className = "code-block"

          const toolbar = document.createElement("div")
          toolbar.className = "code-toolbar"

          const label = document.createElement("span")
          label.className = "code-label"
          label.textContent = title || pre.dataset.language || "code"

          const button = document.createElement("button")
          button.className = "code-copy"
          button.type = "button"
          button.setAttribute("aria-label", "Copy code")
          button.textContent = "Copy"
          button.addEventListener("click", async () => {
            try {
              await navigator.clipboard.writeText(pre.textContent || "")
              button.textContent = "Copied"
            } catch {
              button.textContent = "Failed"
            } finally {
              window.setTimeout(() => {
                button.textContent = "Copy"
              }, 1400)
            }
          })

          toolbar.append(label, button)
          pre.replaceWith(wrapper)
          wrapper.append(toolbar, pre)
        })
      }

      enhanceCodeBlocks()
      document.addEventListener("astro:page-load", enhanceCodeBlocks)
    </script>
  </body>
</html>
